# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: 7807bb8f069d1d8b9cb59df88bd456fb56d330ff21e277358aaaf0daf1e113ff14f332dc6c31663950f7fbbcd3188a412ae7862e38f3be75a63da12d8b51f6b7
  twitter:
    consumer_key: <%= ENV['TWITTER_API_KEY'] %>
    consumer_secret: <%= ENV['TWITTER_API_SECRET'] %>
    access_token: <%= ENV['TWITTER_ACCESS_TOKEN'] %>
    access_token_secret: <%= ENV['TWITTER_ACCESS_TOKEN_SECRET'] %>



test:
  secret_key_base: 0927ccff60b915d096632c5a3a9385a2c1c46e3eb87e4e9db302767aede3f65938671cfbbc927db8c4a0341cd2c8122f822175e009df6341d3462bd3808b79ae

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
