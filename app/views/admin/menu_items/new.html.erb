<h1>Add Menu Item</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for [:admin, @menu_item] do |f| %>

      <div class="field">
        <%= f.label :name %><br>
        <%= f.text_field :name, autofocus: true, placeholder: "Enter your name", class: "form-control" %>
      </div>

      <div class="field">
        <%= f.label :price %><br>
        <%= f.text_field :price, class: "form-control" %>
      </div>

      <div class="field">
        <%= f.label :description %><br>
        <%= f.text_field :description, placeholder: "Enter descriptions", class: "form-control" %>
      </div>

      <div class="field">
        <%= f.label :vegetarian %><br>
        <%= f.check_box :vegetarian %>
      </div>

      <div class="field">
        <%= f.label(:picture) %><br>
        <%= f.file_field(:picture) %>
      </div><br>

      <div class="form-group">
        <%= f.submit :Submit, class: "btn btn-primary"%>
      </div>

    <% end %>
  </div>
</div>
